version: '3.1'

services:
  web:
    image: cowrvalera/docker-ubuntu-apache-php:22.04_php8.2_ioncube_1.0.0
    restart: unless-stopped
    platform: linux/amd64
    volumes:
      - ./src:/var/www/html
    ports:
      - "127.0.0.1:80:80"